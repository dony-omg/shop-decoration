import Container from "../core/components/Container";

/**
 * The function returns a React component that renders a Container component inside a div.
 * @returns A div element containing a Container component.
 */
export default function EditorPage() {
  return (
    <div>
      <Container />
    </div>
  );
}
